import { Link } from "react-router-dom";

const FindProductIdLink = () => {
    return (
        <Link to="/findProductId">
            <button type="button">Find Product ID</button>
        </Link>
    );
};

export default FindProductIdLink;
