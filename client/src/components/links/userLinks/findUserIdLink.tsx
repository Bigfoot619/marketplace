import { Link } from "react-router-dom";

const FindUserIdLink = () => {
    return (
        <Link to="/findUserId">
            <button type="button">Find User ID</button>
        </Link>
    );
};

export default FindUserIdLink;
